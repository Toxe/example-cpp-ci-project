version: 2.1

orbs:
  win: circleci/windows@2.4.0

jobs:
  linux-gcc:
    docker:
      - image: gcc:9
    steps:
      - checkout
      - run: apt-get update
      - run: apt-get install -y ninja-build cmake
      - run: env|sort
      - run: ls -l /usr/bin
      - run: ls -l /usr/local/bin
      - run: gcc --version
      - run:
          command: |
            pushd $HOME
            git clone --depth 1 https://github.com/Microsoft/vcpkg.git vcpkg
            cd vcpkg
            ./bootstrap-vcpkg.sh --disableMetrics
            ./vcpkg install $(< $CIRCLE_WORKING_DIRECTORY/.vcpkg)
            popd
            mkdir build && cd build
            cmake -G Ninja -DCMAKE_BUILD_TYPE=Release -DCMAKE_TOOLCHAIN_FILE=$HOME/vcpkg/scripts/buildsystems/vcpkg.cmake ..
            cmake --build .

  linux-clang:
    docker:
      - image: ubuntu:18.04
    steps:
      - checkout
      - run: apt-get update
      - run: apt-get install -y ninja-build cmake clang-9
      - run: env|sort
      - run: ls -l
      - run: ls -l $HOME
      - run: ls -l /usr/bin
      - run: ls -l /usr/local/bin
      - run: clang++-9 --version
      - run:
          command: |
            pushd $HOME
            git clone --depth 1 https://github.com/Microsoft/vcpkg.git vcpkg
            cd vcpkg
            CC=clang-9 CXX=clang++-9 ./bootstrap-vcpkg.sh --disableMetrics
            CC=clang-9 CXX=clang++-9 ./vcpkg install $(< $CIRCLE_WORKING_DIRECTORY/.vcpkg)
            popd
            mkdir build && cd build
            CC=clang-9 CXX=clang++-9 cmake -G Ninja -DCMAKE_BUILD_TYPE=Release -DCMAKE_TOOLCHAIN_FILE=$HOME/vcpkg/scripts/buildsystems/vcpkg.cmake ..
            cmake --build .

  windows-msvc:
    executor: win/default
    steps:
      - checkout
      - run:
          command: |
            $ProgressPreference = "SilentlyContinue"
            Invoke-WebRequest -URI https://github.com/Kitware/CMake/releases/download/v3.16.4/cmake-3.16.4-win64-x64.zip -OutFile cmake-3.16.4-win64-x64.zip
      - run:
          command: |
            $ProgressPreference = "SilentlyContinue"
            Expand-Archive cmake-3.16.4-win64-x64.zip
      - run: .\cmake-3.16.4-win64-x64\cmake-3.16.4-win64-x64\bin\cmake.exe --version
      - run: Get-ChildItem "Env:" | Sort Name
      - run:
          command: |
            New-Alias -Name git -Value "$Env:ProgramFiles\Git\bin\git.exe"
            New-Alias -Name cmake -Value "$Env:HOMEPATH\cmake-3.16.4-win64-x64\cmake-3.16.4-win64-x64\bin\cmake.exe"
            cd $Env:HOMEPATH
            git clone --quiet --depth 1 https://github.com/Microsoft/vcpkg.git vcpkg-msvc
            cd vcpkg-msvc
            .\bootstrap-vcpkg.bat -disableMetrics
            $packages = Get-Content "$Env:CIRCLE_WORKING_DIRECTORY\.vcpkg"
            .\vcpkg install --triplet x64-windows $packages
            cd $Env:CIRCLE_WORKING_DIRECTORY
            mkdir build-msvc
            cd build-msvc
            cmake -DCMAKE_BUILD_TYPE=Release -DVCPKG_TARGET_TRIPLET=x64-windows -DCMAKE_TOOLCHAIN_FILE="$Env:HOMEPATH\vcpkg-msvc\scripts\buildsystems\vcpkg.cmake" ..
            cmake --build .


workflows:
  version: 2
  run-all:
    jobs:
      - linux-gcc
      - linux-clang
      - windows-msvc
