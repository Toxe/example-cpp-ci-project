version: 2.1

orbs:
  win: circleci/windows@2.4.0

jobs:
  linux-gcc:
    docker:
      - image: gcc:9
    steps:
      - checkout
      - run: apt search ninja
      - run: apt search cmake
      - run: apt-get install ninja-build cmake
      - run: env|sort
      - run: ls -l /usr/bin
      - run: ls -l /usr/local/bin
      - run: gcc --version
      - run:
          command: |
            pushd $HOME
            git clone --depth 1 https://github.com/Microsoft/vcpkg.git vcpkg
            cd vcpkg
            ./bootstrap-vcpkg.sh --disableMetrics
            ./vcpkg install $(< $CIRCLE_WORKING_DIRECTORY/.vcpkg)
            popd
            mkdir build && cd build
            cmake -G Ninja -DCMAKE_BUILD_TYPE=Release -DCMAKE_TOOLCHAIN_FILE=$HOME/vcpkg/scripts/buildsystems/vcpkg.cmake ..
            cmake --build .

  linux-clang:
    docker:
      - image: ubuntu:18.04
    steps:
      - checkout
      - run: apt search ninja
      - run: apt search cmake
      - run: apt search clang
      - run: apt-get install ninja-build cmake clang
      - run: env|sort
      - run: ls -l
      - run: ls -l $HOME
      - run: ls -l /usr/bin
      - run: ls -l /usr/local/bin
      - run: clang++ --version

  windows-msvc:
    executor: win/default
    steps:
      - checkout
      - run: Invoke-RestMethod -Uri https://github.com/Kitware/CMake/releases/download/v3.16.4/cmake-3.16.4-win64-x64.zip -OutFile cmake-3.16.4-win64-x64.zip
      - run: Expand-Archive cmake-3.16.4-win64-x64.zip
      - run: .\cmake-3.16.4-win64-x64\cmake-3.16.4-win64-x64\bin\cmake.exe --version
      - run: CMAKE_EXE="$Env:HOMEPATH\cmake-3.16.4-win64-x64\cmake-3.16.4-win64-x64\bin\cmake.exe"
      - run: Get-ChildItem "Env:" | Sort Name
      - run:
          command: |
            New-Alias -Name git -Value "$Env:ProgramFiles\Git\bin\git.exe"
            cd $Env:HOMEPATH
            git clone --quiet --depth 1 https://github.com/Microsoft/vcpkg.git vcpkg-msvc
            cd vcpkg-msvc
            .\bootstrap-vcpkg.bat -disableMetrics
            $packages = Get-Content "$Env:CIRCLE_WORKING_DIRECTORY\.vcpkg"
            .\vcpkg install --triplet x64-windows $packages
            cd $Env:CIRCLE_WORKING_DIRECTORY
            mkdir build-msvc
            cd build-msvc
            "$Env:HOMEPATH\cmake-3.16.4-win64-x64\cmake-3.16.4-win64-x64\bin\cmake.exe" -DCMAKE_BUILD_TYPE=Release -DVCPKG_TARGET_TRIPLET=x64-windows -DCMAKE_TOOLCHAIN_FILE="$Env:HOMEPATH\vcpkg-msvc\scripts\buildsystems\vcpkg.cmake" ..
            "$Env:HOMEPATH\cmake-3.16.4-win64-x64\cmake-3.16.4-win64-x64\bin\cmake.exe" --build .


workflows:
  version: 2
  run-all:
    jobs:
      - linux-gcc
      - linux-clang
      - windows-msvc
